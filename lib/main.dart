import 'bootstrap.dart';

/// Entry point. Keep this file empty of logic — all startup work belongs in
/// `bootstrap.dart` so it can be reused by integration tests and any future
/// flavour-specific entry points (`main_dev.dart`, `main_prod.dart`).
void main() => bootstrap();
